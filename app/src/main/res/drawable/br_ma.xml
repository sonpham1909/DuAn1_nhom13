<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/black" /> <!-- Đặt màu nền là transparent -->
    <corners android:radius="8dp" /> <!-- Đặt độ cong viền -->
    <stroke
        android:width="1dp"
        android:color="@color/black" /> <!-- Đặt màu viền -->
</shape>